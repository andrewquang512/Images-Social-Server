const Level = {
  // userID: async (parent, args, { prisma }, info) => {
  //   const result = await prisma.level.findUnique({
  //     where: {
  //       id: parent.userID,
  //     },
  //   });
  //   return result;
  // },
};

export default Level;
